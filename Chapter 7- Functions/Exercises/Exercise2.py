# Write a function called favorite_book() that accepts one parameter, title. The function should print a message, such as One of mynfavorite books is Alice in Wonderland. Call the function, making sure to include a book title as an argument in the function call.

def favoritebook(title): # Creates a function.
    print (f"My favorite book is called {title}") # Uses an f-string to format the statement and prints it.

favoritebook('Magnus Chase') # Calls the function and adds the string to its parameters.
