# Write a function called display_message() that prints one sentence telling everyone what you are learning about in this chapter. Call the function, and make sure the message displays correctly.

def message(): # Creates a function
    message = "In this chapter, we are learning about Functions." # Stores the string in a variable.
    print (message) # Prints the variable where the string is stored.

message() # Calls the function, printing the message.